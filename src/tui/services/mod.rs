// TUI services — business logic with no UI dependency.
// These modules handle I/O, process management, and async operations.
// They are consumed by Components via the Action dispatch system.

pub mod java_manager;
